<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/players.css" />
</head>
<body>
    <h1>Search Available Players</h1>
    <div id="filterBar">
        <form id="filterButtons" action="/rosters/search" method="POST">
            <span>Player Name: </span><input type="text" name='name' value="<%= lastValue %>" />
            <input type="submit" value="Search for Player">
        </form>
    </div>
    <div id="playerScroll">
        <table id="playerTable">
            <tr id="playerHeader">
                <th class="name">Player</th>
                <th class="pos">Position</th>
                <th class="team">Team</th>
                <th></th>
            </tr>
            <% for(let i = 0; i < player.length; i++) { %>
                <% if(player[i].roster_id == null || player[i].roster_id == 0) { %>
                    <tr class="playerInfo">
                        <td class="name"><%= player[i].name %></td>
                        <td class="pos"><%= player[i].position %></td>
                        <td class="team"><%= player[i].team %></td>
                        <form action="/rosters/addPlayer/<%=player[i].id%>?_method=PUT" method="POST">
                            <td class="addPlayer"><input  type="submit" value="Add Player" /></td>
                        </form>
                    </tr>
                <% } %>
            <% } %>
        </table> 
    </div>
</body>
</html>